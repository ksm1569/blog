server:
  port: 4000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smsoft?serverTimezone=Asia/Seoul
    username: sumin
    password: sumin1234
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB
     # location: /Users/sumin/uploadfiles/

logging:
  level:
    org.hibernate.type: trace # 콘솔창에 바인딩 되는 값 출력 및 조회 결과 출력
